# Social Consensus Agent - Final Demo Dependencies

# Core (required)
pydantic>=2.0.0
python-dotenv>=1.0.0

# Web API (required for frontend)
fastapi>=0.100.0
uvicorn>=0.23.0

# HTTP (for MCP tools)
requests>=2.28.0

# LLM (optional - for DeepSeek integration)
openai>=1.0.0

# Vector Store (optional - for persistent memory)
chromadb>=0.4.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
